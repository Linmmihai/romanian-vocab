# 罗马尼亚语词库重建与对抗审查报告（2026-08-12）

这次重建最终采用“一套主数据、四个学习入口”的方案。数据库继续保留全部 4,867 个稳定词条编号，已有学习进度、每日队列和离线待同步记录不因分层变化而失去对应关系；默认学习入口缩小为新闻通用核心，生活场景和低频术语只在主动切换词书时出现。

## 当前词书结构

| 主学习轨道 | 词条数 | 使用方式 |
| --- | ---: | --- |
| 新闻通用核心 | 1,789 | 默认课程；通用新闻词、跨栏目词和已核验核心表达 |
| 新闻扩展词 | 588 | 识读扩展；中频词和仍需进一步义项核验的新闻词 |
| 生活场景词书 | 1,254 | 办事、就医、出行、服务和日常沟通时按需学习 |
| 专项术语 | 1,236 | 口译或专业任务前，按门类集中学习 |

专项入口分为法律与公共事务、经济与金融、医疗与健康、科技与工程、能源环境与农业、国防与安全、企业与管理、历史文化与艺术、教育与语言、哲学与思想十本词书。主学习轨道决定词条在默认课程中的位置，专项门类则是额外索引：新闻核心词可以同时出现在相应专项入口中，但不会因此复制词条或拆散学习进度。

## 对抗审查发现与修正

第一版分层曾把自动词形还原后的整组频率分配给具体词义。这个方法在 `sine`、`aplică`、`a ara`、`vin`、`pui`、`cui` 等同形或近形项目上被反例推翻：新闻语料证明的是另一个词性或义项常见，不能证明学习卡上的中文义项常见。第二轮因此改为优先统计词头的精确表面形式；只有精确形式不存在且没有明显歧义时，才允许使用自动词元证据。未完成词性和义项核验的自动归并项不再凭共享频率进入新闻核心。

同一个原则也用于自然度状态。语料中出现某个词元，并不能自动证明当前中文释义、例句和搭配已经自然，因此先前 1,712 条自动写入的 `corpus_attested` 声明已经全部撤回。现在只有人工核验或人工修订的 283 条保留“已确认”状态，其余 4,584 条明确保持 `needs_review`；这不会阻止按需使用，但不会再把未经逐义审核的内容包装成已经验证。

第二轮还累计完成了 81 个稳定编号的人工修订。除拼写、变音和固定搭配外，重点是把高频词从冷门义项拉回新闻主流义，例如 `celălalt` 从哲学“他者”改为“另一个；其余的；对方”，`a recunoaște` 从军事“侦察”改为“认出；承认；认可”，`modalitate` 从哲学“模态”改为“方式；办法”，`a juca un rol` 增补新闻中最常用的“发挥作用”。`hermeneutică`、`transcendență`、`teoremă`、`endocrinologie` 等窄领域项目则迁入相应专项词书。所有这些修改都保留原 `word_id`。

## 学习与同步行为

应用默认选择“新闻通用核心”。新词先展示人工确认的核心表达和已核验常用词，再使用可靠的频率与新闻覆盖排序；有歧义且未经人工核验的卡不会抢在首批出现。今日队列、错词、测验和词表都遵守当前词书范围，界面原来容易误解为跨全库的“全库待复习”也已改成“本词书待加强”。

对抗审查同时发现，专项词书队列在某条云同步路径中可能丢失词书编号，最终被误写为新闻核心，而回读验证又可能仍显示成功。现在每次保存、手动同步、备份和回读核验都携带词书编号；缺失该字段会明确报错，不再静默降级。切换词书会先串行保存旧队列，再建立新队列；快速连续切换、旧请求晚到和跨词书撤销均有隔离，词表渲染还会再次过滤不属于当前词书的项目。

## 数据安全与验证

本轮没有新增用户已拒绝的数据库防护层，也没有删除稳定编号。数据库原有私有快照仍保留 4,867 条重建前数据，可在异常时回退。最终发布要求云端仍为 4,867 个编号、2,024 条既有学习进度不减少、进度外键孤儿为零，并且云端、网页离线包和线上实际资源的词书计数完全一致。

本地 25 组数据、调度与同步检查已经全部通过，浏览器端 37 个真实流程测试也全部通过，覆盖离线答题、固定今日队列、跨设备原子合并、云端回读、快速点击、跨词书撤销、专项词书按需进入和移动端布局。详细逐条修改见 `artifacts/vocab-rebuild-report-20260812.json`，第二轮反例与不变量见 `artifacts/vocab-adversarial-audit-20260812.json`；可复现分层脚本为 `tools/build_vocab_learning_tracks.py`，完整性检查为 `tools/verify_learning_tracks.js` 与 `tools/verify_adversarial_vocab.js`。
